import SettingsForm from '@/components/user/profile/settings/settingsForm';
import { Stack } from '@mui/material';

export default function SettingsPage() {
  return (
    <Stack spacing={4}>
      <SettingsForm />
    </Stack>
  );
}
